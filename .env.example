# GitHub Webhook Server Configuration

# Server Configuration
PORT=3000
NODE_ENV=development

# GitHub Webhook Secret (IMPORTANT: Set this to match your GitHub webhook secret)
# Generate a strong secret and use the same value in GitHub webhook settings
WEBHOOK_SECRET=your_webhook_secret_here

# Example: WEBHOOK_SECRET=mysupersecretkey123456789

# Logging Configuration
LOG_LEVEL=info

# Optional: Database Configuration (if you want to store webhook events)
# DATABASE_URL=postgresql://username:password@localhost:5432/webhooks

# Optional: Notification Settings (for alerts)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
# EMAIL_SERVICE_API_KEY=your_email_service_api_key